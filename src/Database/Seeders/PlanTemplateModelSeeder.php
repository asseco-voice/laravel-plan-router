<?php

namespace Asseco\PlanRouter\Database\Seeders;

use Asseco\PlanRouter\App\Models\PlanTemplate;
use Illuminate\Database\Seeder;

class PlanTemplateModelSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        //$templates = PlanTemplate::all();
        //$models = EmailModel::all('id')->pluck('id')->toArray();
//
        ///**
        // * @var $template PlanTemplate
        // */
        //foreach ($templates as $template) {
        //    $modelNumber = rand(1, 3);
//
        //    $modelRand = [];
        //    for ($i = 0; $i < $modelNumber; $i++) {
        //        $modelRand[] = $models[array_rand($models)];
        //    }
//
        //    $template->models()->sync($modelRand);
        //}
    }
}
